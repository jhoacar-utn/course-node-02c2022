name: telegram message
on: 
  push:
    branches:
      - master
jobs:
  notify:
    name: notify
    runs-on: ubuntu-latest
    steps:
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: html
          message: |
            <b>${{ github.actor }}</b> ha subido un nuevo cambio \xF0\x9F\x98\x81!
            <b>mensaje</b>: ${{ github.event.commits[0].message }}

            <b>repositorio</b>: ${{ github.repository }}

            Puedes verlo acá: https://github.com/${{ github.repository }}/commit/${{github.sha}}
